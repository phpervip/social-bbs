// User Remote entry — this remote is consumed by the Shell host at runtime.
// The exposed components (Auth, Profile) are lazy-loaded by the host via
// Module Federation, so no app bootstrap is needed here.
