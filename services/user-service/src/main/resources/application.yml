server:
  port: 9001

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: user
    password: user123
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrap-servers: localhost:9092

grpc:
  server:
    port: 9001

jwt:
  secret: ${JWT_SECRET:dev-secret}
  ttl-seconds: ${JWT_TTL_SECONDS:86400}
